const fs = require('fs')
fs.stat('C:\Users\charumathi.hariharan\escape-seq-colors.txt', (err, stats) => {
  if (err) {
    console.error(err)
    return
  }

  stats.isFile() //true
  stats.isDirectory() //false
  stats.isSymbolicLink() //false
  stats.size //1024000 //= 1MB
})
